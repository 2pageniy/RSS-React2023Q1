import React, { FC } from 'react';
import SearchCards from '../../widgets/search-cards/ui';

export const Main: FC = () => {
  return (
    <div>
      <SearchCards />
    </div>
  );
};

export default Main;
