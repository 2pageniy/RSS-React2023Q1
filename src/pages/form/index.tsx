import React, { Component } from 'react';
import { FormCard } from '../../features';

export class Form extends Component {
  render() {
    return (
      <>
        <FormCard />
      </>
    );
  }
}

export default Form;
